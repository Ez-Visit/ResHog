<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:ResHog.UI.ViewModels"
             xmlns:cvt="using:ResHog.UI.Converters"
             xmlns:dto="using:ResHog.Shared.Dtos"
             x:Class="ResHog.UI.Views.AlertView"
             x:DataType="vm:AlertViewModel">

    <Grid RowDefinitions="Auto,Auto,*">

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="#FAFAFA" Padding="16,10">
            <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                <TextBlock Text="范围:" VerticalAlignment="Center" FontSize="13" />
                <ComboBox SelectedItem="{Binding SelectedRangeOption}"
                          ItemsSource="{Binding RangeOptions}"
                          MinWidth="140">
                    <ComboBox.ItemTemplate>
                        <DataTemplate x:DataType="vm:NamedOption">
                            <TextBlock Text="{Binding Display}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBlock Text="级别:" VerticalAlignment="Center" FontSize="13" />
                <ComboBox SelectedItem="{Binding SelectedSeverityOption}"
                          ItemsSource="{Binding SeverityOptions}"
                          MinWidth="120">
                    <ComboBox.ItemTemplate>
                        <DataTemplate x:DataType="vm:NamedOption">
                            <TextBlock Text="{Binding Display}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <Button Content="刷新"
                        Command="{Binding LoadAlertsCommand}"
                        IsEnabled="{Binding !IsLoading}"
                        Padding="16,6" />

                <ProgressBar IsVisible="{Binding IsLoading}"
                             IsIndeterminate="True"
                             Width="80"
                             Height="4" />
            </StackPanel>
        </Border>

        <!-- Summary -->
        <Border Grid.Row="1" Background="White" Padding="16,10"
                BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Spacing="32">
                <StackPanel Spacing="2">
                    <TextBlock Text="总计" FontSize="11" Foreground="#888" />
                    <TextBlock Text="{Binding TotalCount, StringFormat='{}{0} 条'}"
                               FontSize="20" FontWeight="Bold" Foreground="#333" />
                </StackPanel>
                <StackPanel Spacing="2">
                    <TextBlock Text="严重" FontSize="11" Foreground="#888" />
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <TextBlock Text="🔴" FontSize="16" />
                        <TextBlock Text="{Binding CriticalCount, StringFormat='{}{0} 条'}"
                                   FontSize="20" FontWeight="Bold" Foreground="#E24B4A" />
                    </StackPanel>
                </StackPanel>
                <StackPanel Spacing="2">
                    <TextBlock Text="警告" FontSize="11" Foreground="#888" />
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <TextBlock Text="🟡" FontSize="16" />
                        <TextBlock Text="{Binding WarningCount, StringFormat='{}{0} 条'}"
                                   FontSize="20" FontWeight="Bold" Foreground="#EF9F27" />
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Alert list -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Alerts}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserResizeColumns="True"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="1"
                  BorderBrush="#E0E0E0"
                  Margin="16,8">
            <DataGrid.Columns>
                <!-- UIW-8(用户澄清追加):时间列 180→220——Timestamp 含 7 位小数秒
                     (26 字符),原宽显示不完整 -->
                <DataGridTemplateColumn Header="时间" Width="220" SortMemberPath="Timestamp">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:DataType="dto:AlertDto">
                            <TextBlock Text="{Binding Timestamp}"
                                       ToolTip.Tip="{Binding Timestamp}"
                                       TextTrimming="CharacterEllipsis" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="进程名" Width="180" SortMemberPath="ProcessName">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:DataType="dto:AlertDto">
                            <TextBlock Text="{Binding ProcessName}"
                                       ToolTip.Tip="{Binding ProcessName}"
                                       TextTrimming="CharacterEllipsis" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="PID" Width="70" SortMemberPath="Pid">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:DataType="dto:AlertDto">
                            <TextBlock Text="{Binding Pid}"
                                       ToolTip.Tip="{Binding Pid}"
                                       TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="服务名" Width="140" SortMemberPath="ServiceName">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:DataType="dto:AlertDto">
                            <TextBlock Text="{Binding ServiceName}"
                                       ToolTip.Tip="{Binding ServiceName}"
                                       TextTrimming="CharacterEllipsis" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="指标" Width="100" SortMemberPath="Metric">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:DataType="dto:AlertDto">
                            <TextBlock Text="{Binding Metric, Converter={x:Static cvt:MetricNameConverter.Instance}}"
                                       ToolTip.Tip="{Binding Metric, Converter={x:Static cvt:MetricNameConverter.Instance}}"
                                       TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="当前值" Width="90" SortMemberPath="Value">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:DataType="dto:AlertDto">
                            <TextBlock Text="{Binding Value, StringFormat=F1}"
                                       ToolTip.Tip="{Binding Value, StringFormat=F1}"
                                       TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="阈值" Width="90" SortMemberPath="Threshold">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:DataType="dto:AlertDto">
                            <TextBlock Text="{Binding Threshold, StringFormat=F1}"
                                       ToolTip.Tip="{Binding Threshold, StringFormat=F1}"
                                       TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="级别" Width="80" SortMemberPath="Severity">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:DataType="dto:AlertDto">
                            <TextBlock Text="{Binding Severity, Converter={x:Static cvt:SeverityNameConverter.Instance}}"
                                       ToolTip.Tip="{Binding Severity, Converter={x:Static cvt:SeverityNameConverter.Instance}}"
                                       TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>

</UserControl>
